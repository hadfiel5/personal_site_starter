
# Frontend (Next.js + Tailwind)

## Prereqs
- Node.js 18+
- npm (or pnpm/yarn)

## Setup
```bash
cd frontend
npm install
# Copy env
cp .env.example .env.local
npm run dev
```

Site runs at `http://127.0.0.1:3000`. Ensure backend is on `http://127.0.0.1:8000`.
